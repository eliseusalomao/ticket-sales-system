export type EventModel = {
    id: string
    name: string
    organization: string
    date: string
    price: number
    image_url: string
    location: string
    rating: number
}

export type SpotModel = {
    id: string
    name: string
    status: string
}